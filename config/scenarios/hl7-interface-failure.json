{
  "name": "HL7 Interface Failure \u2014 Mirth Queue Backup",
  "description": "Switch port errors on HL7 VLAN cause Mirth Connect queue backup. FHIR API errors spike, ETL jobs fail, HL7 delivery stops. Integration page turns RED.",
  "volume_multiplier": 1.2,
  "max_sessions": 20,
  "network_correlation": {
    "enabled": true,
    "description": "CRC errors on HL7 VLAN switch port cause TCP retransmissions and MLLP timeouts",
    "events": [
      {
        "offset_seconds": 0,
        "device": "kcrmc-dist-epic-01",
        "event_type": "interface_errors",
        "params": {
          "interface": "GigabitEthernet1/0/24",
          "crc_errors": 800
        }
      },
      {
        "offset_seconds": 30,
        "device": "kcrmc-dist-epic-01",
        "event_type": "port_flap",
        "params": {
          "interface": "GigabitEthernet1/0/24"
        }
      }
    ]
  }
}